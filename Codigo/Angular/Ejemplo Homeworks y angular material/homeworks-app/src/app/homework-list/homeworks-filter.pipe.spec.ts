import { HomeworksFilterPipe } from './homeworks-filter.pipe';

describe('HomeworksFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HomeworksFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
